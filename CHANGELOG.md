# PaperCraft 更新日志

## v1.1.8 (2026-09-05)

### 修复（社区审核二次响应）
- **`obsidianmd/settings-tab/no-deprecated-display` Error**：彻底移除对 `display()` 的覆写与
  `eslint-disable` 注释，将设置面板迁移至 Obsidian 1.13.0+ 声明式设置 API——
  `getSettingDefinitions()` 返回单个 `render` 类型定义，render 回调把框架生成的 Setting 行
  改造成全宽宿主容器，实时预览、标签切换、模板管理等复杂交互逻辑保持不变
  （此前靠注释屏蔽规则的做法不被允许，现为真正迁移）
- **`Unexpected undescribed directive comment` Error**：随之消除（不再存在任何 eslint-disable 注释）

### 其他
- 声明式宿主容器新增 `.papercraft-custom-setting` / `.papercraft-custom-host` 样式

---

## v1.1.7 (2026-09-05)

### 修复（社区审核响应）
- **`obsidian.md/ui/heading` Error**：字体排版页「页面边距」区块由 `createEl('h3')` 改为 `new Setting().setHeading()`，与官方 UI 风格保持一致
- **`obsidianmd/prefer-create-el` Warning**：模板列表项元素改用 `createDiv()` 替代 `createEl('div')`
- **`obsidianmd/prefer-modal-confirm` Warning**：删除模板确认对话框由 `window.confirm()` 改为两段式 armed 按钮（首次点击进入 armed 态，5 秒内再次点击才真正删除；超时自动解除）

### 其他
- `display()` deprecation 警告：因本面板含实时预览与模板保存等复杂自定义组件，无法用 1.13+ 声明式 API 表达，已在文件内主动 disable 该规则，并附详细注释说明

---

## v1.1.6 (2026-09-03)

### 新增
- **页面边距客制化**：设置面板「字体排版」标签页新增「页面边距」区块，支持独立调整上 / 下 / 左 / 右边距（0–600px）
  - 数值输入即时反馈到设置页实时预览区
  - 点击「应用到笔记」后对编辑 / 阅读视图生效
  - 与内置模板的默认边距完全兼容，可在模板基础上微调

---

## v1.1.5 (2026-08-16)

### 修复
- **背景渲染问题**：修复横线/竖线/网格/点状模板的背景渲染错误
  - 横线模板：使用 `100% × gap` 尺寸，避免 `auto` 导致的纵向色块
  - 竖线模板：使用 `gap × 100%` 尺寸
  - 网格模板：使用 `gap × gap` 尺寸
  - 点状模板：使用 `gap × gap` 尺寸，确保点在 Chromium 下正确重复
- **点状透明度**：将所有点状模板的颜色透明度从 0.30-0.45 降低到 0.15-0.25，减少视觉干扰，不影响阅读

### 影响模板
- 星点、旧卷星砂、落英缤纷、晴空碎玉、素笺白露、淡紫微雨、炭灰星点、暖黄流光、桃花拾遗、雪泥鸿爪
- 冬夜旅人、荷塘月色、碧海青天（夜间主题）

---

## v1.1.4 (2026-08-16)

### 修复
- **横线渲染 bug**：修复 `CSSGenerator.ts` 中 `buildBackgroundSize` 方法对横线模板返回 `100% 100%` 导致的渲染错误
- 横线模板现在使用 `auto` 尺寸，保持 `repeating-linear-gradient` 按间距正确重复

---

## v1.1.3 (2026-08-16)

### 新增
- **模板重设计**：
  - 窗外麻雀：墨蓝色横线 + 偏天蓝色背景
  - 绿肥红瘦：紫红色字体 (#7B2D5E)
- **侧边栏 UI 优化**：
  - 分组标题左侧竖线装饰
  - 卡片阴影与悬停效果优化
  - 夜间主题提示改为浅灰色，放在标题同行
  - 自定义模板空状态提示放在标题括号内
- **按钮样式**：
  - 清除格式图标改为浅灰色
  - 应用到笔记按钮去掉蓝色高亮，与其他按钮统一

---

## v1.1.2 (2026-08-15)

### 模板更新
- 新增 6 个模板：雪泥鸿爪、禅房花木、荷塘月色、冬夜旅人、绿肥红瘦、埋首故纸
- 删除 2 个模板：现代极简、薄荷横线
- 重命名：玫瑰落英 → 落英缤纷、豆沙清梦 → 莲叶田田、墨蓝横线 → 窗外麻雀、古风遗韵 → 埋首故纸
- 玄妙之门 → 荷塘月色、月明星稀 → 冬夜旅人

### UI 改进
- 内置模板分为「白天主题」和「夜间主题」两组显示
- 夜间主题添加「建议在深色模式下使用」提示
- 自定义模板空状态显示「暂无自定义模板」

---

## v1.1.0 - v1.1.1 (2026-08-14)

### 新增功能
- 模板管理系统（保存/加载/删除自定义模板）
- 实时预览区（设置面板内）
- 导出/导入模板功能
- 绘图批注功能（画笔/荧光笔/橡皮擦/形状）

### UI 优化
- 侧边栏模板列表优化
- 设置面板标签导航
- 操作按钮竖向排列

---

## v1.0.0 - v1.0.7 (2026-08-09 ~ 2026-08-13)

### 基础功能
- 自定义稿纸样式（背景色/纹理/线条/字体/边距）
- 12 个内置模板（古风/现代/简约风格）
- 实时预览
- 响应式设计
